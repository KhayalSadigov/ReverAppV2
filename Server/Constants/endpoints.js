const endpoints = {
    users : {
        getAll : '/api/users',
        getOne : '/api/users/:id',
        post : '/api/users',
        delete : '/api/users/:id',
        patch : '/api/users/:id',
    }
}

module.exports = endpoints